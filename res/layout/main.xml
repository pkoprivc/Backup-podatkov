<?xml version="1.0" encoding="utf-8"?>

<LinearLayout android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:clipChildren="false" 
    android:layout_gravity="center|bottom" 
    xmlns:android="http://schemas.android.com/apk/res/android">
    <LinearLayout 
    android:id="@+id/linearLayout1" 
    android:layout_width="match_parent" 
    android:layout_height="wrap_content" android:layout_gravity="top|bottom" android:layout_weight="100">
        <CheckedTextView android:text="CheckedTextView" 
        android:layout_height="wrap_content" 
        android:layout_width="match_parent" 
        android:id="@+id/checkedTextView1" 
        android:gravity="top|center" android:layout_gravity="top"></CheckedTextView>
    </LinearLayout>
    <LinearLayout android:id="@+id/linearLayout2" 
    android:layout_width="match_parent" 
    android:layout_height="wrap_content" 
    android:gravity="right" android:layout_weight="1">
        <ImageButton android:layout_width="wrap_content" android:background="@null"  android:layout_height="wrap_content" android:layout_weight="1" android:src="@drawable/search" android:id="@+id/btnSearch" android:onClick="onClickSearch"></ImageButton>
        <ImageButton android:layout_width="wrap_content" android:background="@null"  android:layout_height="wrap_content" android:layout_weight="1" android:src="@drawable/connect" android:id="@+id/btnConnect" android:onClick="onClickConnect"></ImageButton>
    </LinearLayout>
</LinearLayout>
